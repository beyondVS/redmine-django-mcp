[project]
name = "redmine-django-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "httpx",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
